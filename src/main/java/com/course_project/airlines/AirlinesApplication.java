package com.course_project.airlines;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main class for the Airlines application
 * Initializes and runs the Spring Boot application
 */
@SpringBootApplication
public class AirlinesApplication {
	/**
	 * The main entry point of the Airlines application
	 *
	 * @param args - the command line arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(AirlinesApplication.class, args);
	}

}
